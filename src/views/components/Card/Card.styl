@import '~shared-styles/consts.styl'

$card-height = 14px
$card-bg-color = #FBFBFB
$card-border-color = #D7F1D7
$card-text-w = 203px
$card-text-fontsize = 12px


// ------------------------------------------------------------------------------
// Card container

.wrapper
  padding $global-card-v-padding $global-h-board-padding $global-card-v-padding $global-h-board-padding
.container
  display flex
  min-height $card-height + 4
  color $global-c-grey-text
  border solid 1px $card-border-color
  line-height $card-height
  background-color $card-bg-color
  font-size $card-text-fontsize
  cursor grab
  &:active
    cursor grabbing
  &:hover
    box-shadow 0 0 1px 0 rgba(155,155,155,0.5)
  // set darker icon color on hover
  &:hover .icon
    color $global-c-grey-text


// ------------------------------------------------------------------------------
// Card content

.text
  display inline-block
  white-space normal
  flex 1 1 auto
  padding-left 4px

.input
  color $global-c-blue-text
  background transparent
  border none
  // width 200px
  &:focus, &:active
    border none
    outline none

// Card action buttons
.controlsBlock
  padding-left 4px

.icon
  padding 0 5px 0 0
  color $global-c-icon-inactive
  cursor pointer
  // &:hover
  //   text-shadow 0 0 10px #5BE414
  &:active
    color $global-c-icon-active !important


